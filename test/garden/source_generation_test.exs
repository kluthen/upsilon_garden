defmodule UpsilonGarden.Garden.SourceGenerationTest do 
    use ExUnit.Case, async: true

    test "a source does project components in garden according to its parameters" do 

    end

    test "a source with a stone bloc in its range won't be able to project components in that direction" do 

    end

    test "a stone bloc within source range won't be affected by source" do 

    end
end